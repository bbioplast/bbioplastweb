(()=>{
 const main=document.querySelector('#blog-main'),esc=BlogContent.escape;
 const date=v=>new Date(v).toLocaleDateString('th-TH',{day:'numeric',month:'long',year:'numeric'});
 const picture=(p,cls='')=>p.cover?`<img class="${cls}" src="${esc(p.cover)}" alt="${esc(p.title)}" loading="lazy">`:'<div class="blog-art" aria-hidden="true"><span>B&B</span><small>KNOWLEDGE / AUTOMATION</small><i>↗</i></div>';
 const fixImages=()=>main.querySelectorAll('img').forEach(img=>img.addEventListener('error',()=>{img.replaceWith(Object.assign(document.createElement('div'),{className:'blog-art',textContent:'B&B · KNOWLEDGE'}));},{once:true}));
 async function get(url){const response=await fetch(url);if(!response.ok)throw Object.assign(Error('ไม่สามารถโหลดบทความได้'),{status:response.status});return response.json();}
 async function load(){try{
  const slug=location.pathname.startsWith('/blog/')?location.pathname.slice(6):'';
  if(slug){const post=await get('/api/blog/'+encodeURIComponent(slug));document.title=post.title+' | B&B Bioplast Tech';document.querySelector('meta[name=description]').content=post.excerpt||post.title;main.innerHTML=`<article class="blog-article container"><a class="blog-back" href="/blog">← บทความทั้งหมด</a><header class="blog-article-heading"><p class="blog-kicker">${esc(post.category||'B&B INSIGHTS')}</p><h1>${esc(post.title)}</h1><p class="blog-deck">${esc(post.excerpt)}</p><div class="blog-meta"><span>ทีมงาน B&B Bioplast Tech</span><time datetime="${esc(post.published_at)}">${date(post.published_at)}</time></div></header>${post.cover?`<div class="blog-cover">${picture(post)}</div>`:''}<div class="blog-prose">${BlogContent.render(post.content)}</div><aside class="blog-cta"><div><p class="blog-kicker">LET’S FIND YOUR SOLUTION</p><h2>เลือกอุปกรณ์ให้เหมาะกับงานของคุณ</h2><p>ดูสินค้า หรือพูดคุยกับทีมงานเพื่อขอคำแนะนำ</p></div><a class="button" href="/products">ดูสินค้าทั้งหมด ↗</a></aside></article>`;fixImages();return;}
  const {posts}=await get('/api/blog');
  main.innerHTML=`<section class="blog-hero"><div class="container"><p class="blog-kicker">B&B JOURNAL / KNOWLEDGE & IDEAS</p><h1>ความรู้ที่ต่อยอด<br>ทุกกระบวนการ<span>ผลิต.</span></h1><p>แนวคิด เทคนิค และเรื่องน่ารู้ด้าน Automation<br>เพื่อช่วยคุณเลือกอุปกรณ์และพัฒนางานในโรงงาน</p><span class="blog-hero-mark" aria-hidden="true">↗</span></div></section><section class="container blog-library" aria-label="บทความทั้งหมด"><div class="blog-library-head"><div><p class="blog-kicker">LATEST STORIES</p><h2>บทความทั้งหมด</h2></div><label class="blog-search"><span>ค้นหาบทความ</span><input id="blog-search" type="search" placeholder="หัวข้อหรือคำที่สนใจ…"></label></div><div id="blog-categories" class="blog-categories" role="group" aria-label="หมวดหมู่บทความ"></div><p id="blog-count" class="blog-count" role="status"></p><div id="blog-grid" class="blog-grid"></div><div id="blog-empty" class="blog-empty" hidden><span aria-hidden="true">✎</span><h3>${posts.length?'ไม่พบบทความที่ค้นหา':'บทความดี ๆ กำลังตามมา'}</h3><p>${posts.length?'ลองเปลี่ยนคำค้นหาหรือเลือกหมวดหมู่อื่น':'ติดตามเทคนิคและความรู้จากทีมงานได้ที่นี่ ระหว่างนี้เลือกชมอุปกรณ์ที่เหมาะกับงานของคุณ'}</p>${posts.length?'<button id="blog-reset" class="button">แสดงบทความทั้งหมด</button>':'<a class="button" href="/products">สำรวจสินค้าทั้งหมด ↗</a>'}</div></section>`;
  let category='';const names=[...new Set(posts.map(p=>p.category||'ทั่วไป'))];
  const render=()=>{const term=document.querySelector('#blog-search').value.trim().toLowerCase(),filtered=posts.filter(p=>(!category||(p.category||'ทั่วไป')===category)&&(p.title+' '+p.excerpt+' '+p.category).toLowerCase().includes(term));document.querySelector('#blog-categories').innerHTML=['',...names].map(n=>`<button data-category="${esc(n)}" aria-pressed="${category===n}">${esc(n||'ทั้งหมด')}</button>`).join('');document.querySelector('#blog-count').textContent=`${filtered.length} บทความ`;document.querySelector('#blog-empty').hidden=!!filtered.length;document.querySelector('#blog-grid').innerHTML=filtered.map(p=>`<a class="blog-card" href="/blog/${p.slug}"><div class="blog-card-image">${picture(p)}</div><div class="blog-card-copy"><p class="blog-kicker">${esc(p.category||'B&B INSIGHTS')}</p><h3>${esc(p.title)}</h3><p class="blog-excerpt">${esc(p.excerpt||'อ่านบทความจากทีมงาน B&B Bioplast Tech')}</p><div class="blog-card-bottom"><time datetime="${esc(p.published_at)}">${date(p.published_at)}</time><span>อ่านบทความ ↗</span></div></div></a>`).join('');fixImages();};
  document.querySelector('#blog-search').oninput=render;document.querySelector('#blog-categories').onclick=e=>{const button=e.target.closest('[data-category]');if(button){category=button.dataset.category;render();}};if(posts.length)document.querySelector('#blog-reset').onclick=()=>{category='';document.querySelector('#blog-search').value='';render();};render();
 }catch(error){main.innerHTML=`<section class="container blog-empty"><h1>${error.status===404?'ไม่พบบทความนี้':'โหลดบทความไม่สำเร็จ'}</h1><p>${error.status===404?'บทความอาจถูกย้ายหรือยังไม่ได้เผยแพร่':'กรุณาลองอีกครั้ง'}</p><a class="button" href="/blog">บทความทั้งหมด</a> <button id="blog-retry" class="button light">ลองอีกครั้ง</button></section>`;document.querySelector('#blog-retry').onclick=load;}}
 load();
})();
